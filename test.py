import numpy as np

# Create an array of squared numbers
squared_numbers = np.array([2, 3, 4, 5])

# Take the square root of each element in the array
square_root_numbers = np.sqrt(squared_numbers)

print(square_root_numbers)
